# 5.2.2 DOKUMENTASI HALAMAN & ROUTE SISTEM INVENTARIS

## 5.2.2.1 Halaman Login

Halaman login merupakan pintu masuk utama sistem inventaris barang sekolah yang memastikan setiap pengguna terauthentikasi dengan benar sebelum mengakses aplikasi. Pada halaman ini, pengguna (admin, petugas inventaris, atau kepala sekolah) memasukkan kredensial mereka berupa email dan password, kemudian sistem akan memvalidasi data tersebut dengan database. Jika login berhasil, user akan diarahkan ke dashboard admin sesuai dengan rolenya, dan sistem akan mencatat waktu login untuk audit trail. Fitur keamanan seperti password hashing dan rate limiting juga diimplementasikan untuk mencegah brute force attack.

**Route:**

-   GET /admin/login → Menampilkan form login
-   POST /admin/login → Memproses login dan generate session (via Filament)

---

## 5.2.2.2 Halaman Dashboard Admin

Halaman dashboard admin adalah pusat kontrol administrator yang menampilkan ringkasan statistik menyeluruh mengenai kinerja sistem inventaris. Dashboard ini menampilkan widget-widget penting seperti total barang, total kategori, total lokasi, dan total transaksi dalam sistem. Admin dapat melihat grafik trend transaksi, monitoring stok barang yang rendah, dan alert untuk barang dengan kondisi rusak. Setiap widget dapat diklik untuk drill-down ke laporan yang lebih detail dan memanage data terkait.

**Route:**

-   GET /admin → Halaman dashboard utama admin (filament.admin.pages.dashboard)

---

## 5.2.2.3 Halaman Manajemen Kategori Barang

Halaman manajemen kategori memudahkan admin mengorganisir barang ke dalam kategori-kategori yang logis dan mudah dipahami oleh petugas inventaris. Admin dapat membuat kategori baru, edit nama/deskripsi kategori, atau menghapus kategori yang sudah tidak digunakan. Sistem mencegah penghapusan kategori yang masih memiliki barang yang tertaut. Fitur soft delete memastikan data kategori dapat dipulihkan jika terjadi penghapusan yang tidak disengaja, dan setiap perubahan tercatat dalam log aktivitas untuk audit purposes.

**Route:**

-   GET /admin/kategoris → Daftar semua kategori barang (filament.admin.resources.kategoris.index)
-   GET /admin/kategoris/create → Form tambah kategori baru (filament.admin.resources.kategoris.create)
-   POST /admin/kategoris → Simpan kategori baru (via Filament)
-   GET /admin/kategoris/{record}/edit → Form edit kategori (filament.admin.resources.kategoris.edit)
-   PUT/PATCH /admin/kategoris/{record} → Update data kategori (via Filament)
-   DELETE /admin/kategoris/{record} → Hapus kategori (via Filament)

---

## 5.2.2.4 Halaman Manajemen Lokasi Penyimpanan

Halaman manajemen lokasi memudahkan admin mengelola berbagai tempat penyimpanan barang di sekolah seperti gudang, ruang kelas, laboratorium, perpustakaan, dll. Admin dapat membuat lokasi baru, edit informasi lokasi (nama, gedung, lantai, keterangan), atau menghapus lokasi yang sudah tidak digunakan. Setiap lokasi menampilkan informasi detail seperti nama lokasi, gedung, lantai, dan deskripsi lokasi. Sistem mencegah penghapusan lokasi yang masih memiliki barang yang disimpan di dalamnya.

**Route:**

-   GET /admin/lokasis → Daftar semua lokasi penyimpanan (filament.admin.resources.lokasis.index)
-   GET /admin/lokasis/create → Form tambah lokasi baru (filament.admin.resources.lokasis.create)
-   POST /admin/lokasis → Simpan lokasi baru (via Filament)
-   GET /admin/lokasis/{record}/edit → Form edit lokasi (filament.admin.resources.lokasis.edit)
-   PUT/PATCH /admin/lokasis/{record} → Update data lokasi (via Filament)
-   DELETE /admin/lokasis/{record} → Hapus lokasi (via Filament)

---

## 5.2.2.5 Halaman Manajemen Data Barang

Halaman manajemen barang adalah dashboard lengkap untuk admin mengelola semua data barang inventaris yang dimiliki sekolah. Admin dapat melihat tabel barang dengan kolom lengkap seperti kode barang, nama, kategori, lokasi, stok (jumlah), reorder point, satuan, harga, merk, status kondisi, dan tanggal pembelian. Fitur pencarian dan filter memudahkan navigasi dalam katalog barang yang besar. Admin dapat melakukan operasi seperti tambah barang baru, edit informasi barang, melihat detail barang, atau menghapus barang. Setiap barang menampilkan status kondisi (baik/rusak/hilang) dan informasi stok minimum yang harus di-maintain.

**Route:**

-   GET /admin/barangs → Daftar semua barang (filament.admin.resources.barangs.index)
-   GET /admin/barangs/create → Form tambah barang baru (filament.admin.resources.barangs.create)
-   POST /admin/barangs → Simpan barang baru (via Filament)
-   GET /admin/barangs/{record} → Lihat detail barang (filament.admin.resources.barangs.view)
-   GET /admin/barangs/{record}/edit → Form edit barang (filament.admin.resources.barangs.edit)
-   PUT/PATCH /admin/barangs/{record} → Update data barang (via Filament)
-   DELETE /admin/barangs/{record} → Hapus barang (via Filament)

---

## 5.2.2.6 Halaman Manajemen Barang Rusak

Halaman manajemen barang rusak memberikan visibility khusus terhadap barang yang mengalami kerusakan dan perlu ditangani. Admin dapat melihat daftar barang rusak dengan informasi seperti barang yang rusak, lokasi ditemukan, deskripsi kerusakan, tanggal pelaporan, dan status penanganan (pending/diperbaiki/dibuang). Admin dapat melakukan operasi seperti tambah laporan barang rusak baru, edit status penanganan, atau melihat detail barang rusak. Sistem mengintegrasikan dengan master barang untuk mencegah penjualan barang yang rusak.

**Route:**

-   GET /admin/barang-rusaks → Daftar barang rusak (filament.admin.resources.barang-rusaks.index)
-   GET /admin/barang-rusaks/create → Form lapor barang rusak (filament.admin.resources.barang-rusaks.create)
-   POST /admin/barang-rusaks → Simpan laporan barang rusak (via Filament)
-   GET /admin/barang-rusaks/{record}/edit → Form edit status barang rusak (filament.admin.resources.barang-rusaks.edit)
-   PUT/PATCH /admin/barang-rusaks/{record} → Update status barang rusak (via Filament)
-   DELETE /admin/barang-rusaks/{record} → Hapus laporan barang rusak (via Filament)

---

## 5.2.2.7 Halaman Manajemen Transaksi Barang

Halaman manajemen transaksi adalah dashboard lengkap untuk admin mengelola semua pergerakan barang masuk dan keluar dari gudang. Admin dapat melihat daftar transaksi dengan informasi lengkap seperti kode transaksi, jenis transaksi (masuk/keluar), barang terlibat, jumlah, tanggal transaksi, penanggung jawab, keterangan, dan status approval. Fitur pencarian dan filter memudahkan admin menemukan transaksi spesifik berdasarkan periode, jenis, atau status. Admin dapat melakukan operasi seperti tambah transaksi baru, edit transaksi, melihat detail, atau melihat approval history. Sistem otomatis validasi untuk mencegah transaksi keluar yang melebihi stok yang tersedia.

**Route:**

-   GET /admin/transaksi-barangs → Daftar semua transaksi (filament.admin.resources.transaksi-barangs.index)
-   GET /admin/transaksi-barangs/create → Form tambah transaksi baru (filament.admin.resources.transaksi-barangs.create)
-   POST /admin/transaksi-barangs → Simpan transaksi baru (via Filament)
-   GET /admin/transaksi-barangs/{record} → Lihat detail transaksi (filament.admin.resources.transaksi-barangs.view)
-   GET /admin/transaksi-barangs/{record}/edit → Form edit transaksi (filament.admin.resources.transaksi-barangs.edit)
-   PUT/PATCH /admin/transaksi-barangs/{record} → Update data transaksi (via Filament)
-   DELETE /admin/transaksi-barangs/{record} → Hapus transaksi (via Filament)

---

## 5.2.2.8 Halaman Persetujuan Transaksi

Halaman persetujuan transaksi adalah workflow approval untuk semua transaksi barang yang dibuat oleh petugas inventaris dengan status approval yang masih pending. Admin/Kepala Sekolah dapat melihat daftar transaksi pending yang menunggu persetujuan, lengkap dengan detail transaksi seperti barang, jumlah, dan keterangan. Mereka dapat melakukan review dan kemudian approve atau reject transaksi. Jika approve, stok barang akan terupdate dan transaksi menjadi valid. Jika reject, transaksi akan kembali ke status draft dan petugas dapat merevisi.

**Route:**

-   GET /admin/approval-transaksis → Daftar transaksi pending approval (filament.admin.resources.approval-transaksis.index)
-   POST /admin/transaksi-barangs/{record}/approve → Approve transaksi (via Filament action)
-   POST /admin/transaksi-barangs/{record}/reject → Reject transaksi (via Filament action)

---

## 5.2.2.9 Halaman Log Aktivitas Sistem

Halaman log aktivitas menampilkan audit trail lengkap dari semua aksi yang dilakukan pengguna dalam sistem untuk keperluan compliance dan security. Admin dapat melihat daftar lengkap log dengan informasi seperti user yang melakukan aksi, jenis aktivitas (create/read/update/delete/login), tabel yang terpengaruh, record ID, deskripsi aktivitas, perubahan data (dalam format JSON), IP address, user agent, dan timestamp. Fitur filter memudahkan pencarian berdasarkan user, jenis aktivitas, tabel, atau periode waktu. Setiap log adalah read-only untuk menjaga integritas audit trail.

**Route:**

-   GET /admin/log-aktivitas → Daftar semua log aktivitas (filament.admin.resources.log-aktivitas.index)
-   GET /admin/log-aktivitas/create → Form tambah log manual (filament.admin.resources.log-aktivitas.create)
-   POST /admin/log-aktivitas → Simpan log manual (via Filament)
-   GET /admin/log-aktivitas/{record} → Lihat detail log (filament.admin.resources.log-aktivitas.view)
-   GET /admin/log-aktivita/{record}/edit → Form edit log (filament.admin.resources.log-aktivitas.edit)
-   PUT/PATCH /admin/log-aktivitas/{record} → Update log (via Filament)
-   DELETE /admin/log-aktivitas/{record} → Hapus log (via Filament)

---

## 5.2.2.10 Halaman Manajemen Pengguna

Halaman manajemen pengguna memungkinkan admin untuk melakukan CRUD terhadap akun pengguna sistem, baik itu petugas inventaris, kepala sekolah, maupun admin lainnya. Admin dapat melihat daftar lengkap pengguna dengan informasi nama, email, role, status aktif, dan waktu terakhir login. Fitur pencarian dan filter memudahkan admin menemukan user tertentu, sementara tombol action memungkinkan edit profil, assign roles, atau menghapus akun user. Sistem menggunakan Spatie Permission untuk flexible role dan permission management. Setiap perubahan terhadap user tercatat dalam log aktivitas untuk security dan compliance.

**Route:**

-   GET /admin/users → Daftar semua pengguna (filament.admin.resources.users.index)
-   GET /admin/users/create → Form tambah pengguna baru (filament.admin.resources.users.create)
-   POST /admin/users → Simpan pengguna baru (via Filament)
-   GET /admin/users/{record}/edit → Form edit pengguna (filament.admin.resources.users.edit)
-   PUT/PATCH /admin/users/{record} → Update data pengguna (via Filament)
-   DELETE /admin/users/{record} → Hapus pengguna (via Filament)

---

## 5.2.2.11 Halaman Dashboard Laporan (Analytics)

Halaman dashboard laporan adalah halaman analytics komprehensif yang menampilkan statistik dan insight mendalam dari data inventaris. Halaman ini menampilkan berbagai widget analytics seperti total barang per kategori, distribusi barang per lokasi, trend transaksi masuk vs keluar, barang dengan stok rendah, dan statistik barang rusak. Admin dapat melihat visualisasi data dalam bentuk charts dan tables yang interaktif, dan melakukan filter berdasarkan periode waktu. Data analytics ini membantu admin membuat keputusan strategis terkait procurement dan inventory management.

**Route:**

-   GET /admin/laporan-dashboard → Halaman dashboard laporan (filament.admin.pages.laporan-dashboard)

---

## 5.2.2.12 Halaman Backup & Restore

Halaman backup & restore memberikan admin kemampuan untuk membuat backup database sistem dan melakukan restore dari backup file yang sebelumnya dibuat. Admin dapat melihat history semua backup yang telah dibuat dengan informasi seperti filename, ukuran file, waktu backup, status (success/failed), dan user yang melakukan backup. Fitur utama di halaman ini adalah tombol "Create Backup" untuk membuat backup baru dan "Upload & Restore" untuk restore dari file backup. Sistem backup dilakukan dalam format SQL dan disimpan di storage/backups dengan timestamped filename untuk easy reference.

**Route:**

-   GET /admin/backup-manager → Halaman backup & restore (filament.admin.pages.backup-manager)
-   POST /admin/backup-manager → Proses backup (via Filament action)
-   POST /admin/backup-manager/restore → Proses restore dari uploaded file (via Filament action)
-   GET /admin/backups/{id}/download → Download backup file (BackupDownloadController@download)

---

## 5.2.2.13 Halaman Logout

Proses logout dilakukan ketika user ingin keluar dari sistem. Sistem akan menghapus session, invalidate token, dan redirect user kembali ke halaman login. Logout dapat dilakukan melalui menu user di navbar atau hyperlink logout di berbagai halaman sistem. Sistem mencatat waktu logout dalam log aktivitas untuk audit trail.

**Route:**

-   POST /admin/logout → Proses logout user (filament.admin.auth.logout)

---

## Ringkasan Route & Halaman

| No. | Halaman       | URL                        | Roles Akses | Fungsi Utama             |
| --- | ------------- | -------------------------- | ----------- | ------------------------ |
| 1   | Login         | /admin/login               | Public      | Authentication           |
| 2   | Dashboard     | /admin                     | All         | Overview Sistem          |
| 3   | Kategori      | /admin/kategoris           | Admin       | Manage Kategori          |
| 4   | Lokasi        | /admin/lokasis             | Admin       | Manage Lokasi            |
| 5   | Barang        | /admin/barangs             | Admin       | Manage Master Barang     |
| 6   | Barang Rusak  | /admin/barang-rusaks       | Admin       | Track Barang Rusak       |
| 7   | Transaksi     | /admin/transaksi-barangs   | All         | Manage Pergerakan Barang |
| 8   | Approval      | /admin/approval-transaksis | Admin       | Approve Transaksi        |
| 9   | Log Aktivitas | /admin/log-aktivitas       | Admin       | Audit Trail              |
| 10  | Users         | /admin/users               | Admin       | Manage Pengguna          |
| 11  | Laporan       | /admin/laporan-dashboard   | All         | Analytics & Report       |
| 12  | Backup        | /admin/backup-manager      | Admin       | Backup & Restore         |
| 13  | Logout        | /admin/logout              | All         | Keluar Sistem            |

---

## Struktur Role & Permission

**Admin Sistem** → Full access ke semua halaman dan fitur

**Petugas Inventaris** → Akses ke:

-   Dashboard (read-only)
-   Barang (read-only)
-   Transaksi Barang (create/read/edit/delete dengan validation)
-   Barang Rusak (create/read)
-   Laporan Dashboard (read-only)

**Kepala Sekolah** → Akses ke:

-   Dashboard (read-only)
-   Barang (read-only)
-   Transaksi Barang (view/approve)
-   Laporan Dashboard (read-only)

---

**Last Updated:** 29 Desember 2025  
**Versi Laravel:** 12.43.1  
**Status:** Dokumentasi Lengkap Semua Halaman & Route Sistem Inventaris  
**Total Halaman Utama:** 13 halaman  
**Total Route:** 42+ routes (termasuk Filament auto-generated routes)
